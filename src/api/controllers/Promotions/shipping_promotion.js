var module = require('../../models/Promotions/shipping_promotion')

exports.getList = (req,res) =>{
   // console.log("complete list here")
   module.getResult((err,emp)=>{
       if(err)
       {
          res.send(err)
       }
       console.log(emp)
       res.send(emp)
   })
}

exports.getShipping_promotionByID= (req,res)=>{
   module.getShipping_promotionByID(req.params.id,(err,Shipping_promotion)=>{
      if(err)
      {
         res.send(err)
      }
      else{
         console.log("Single Shipping_promotion data",Shipping_promotion)
         res.send(Shipping_promotion)
      }

   })
}


exports.AddShipping_promotion=(req,res)=>{
   const data = new module(req.body)   
   console.log("request data", req.body)

   if(req.body.constructor === Object && Object.keys(req.body).length===0)
   {
      res.send(400).send({success:false,message:"Please fill up all the fields"})
   }
   else{
      module.createShipping_promotion(data,(err,emp)=>{
         if(err)
         {
            res.send(err)
         }
         else{
            res.json({status:true, message:"Success",data:emp.InsertId})
         }
      })
      console.log("valid data")
   }
}


//Update Shipping_promotion model
exports.UpdateShipping_promotion=(req,res)=>{

   const data = new module(req.body)   
   console.log("request data", req.body)

   if(req.body.constructor === Object && Object.keys(req.body).length===0)
   {
      res.send(400).send({success:false,message:"Please fill up all the fields"})
   }
   else{
      module.updateShipping_promotion(req.params.id,data,(err,emp)=>{
         if(err)
         {
            res.send(err)
         }
         else{
            res.json({status:true, message:"Success",data:emp.InsertId})
         }
      })
      console.log("valid data")
   }

}


//Delete Shipping_promotion

exports.deleteShipping_promotion = (req,res) =>{
   module.deleteShipping_promotion(req.params.id,(err,result)=>{
      if (err)
      {
         res.send(err)
      }
      else
      {
         res.json({status:true, message:"Deleted Successfully"})
      }

   })
}